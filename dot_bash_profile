# Fix: Alacritty doesn't inherit User PATH from Windows registry when launched from Start Menu; it has perf impact
# IMPORTANT: This must run BEFORE sourcing .bashrc because .bashrc uses fzf
# Apply for all terminals EXCEPT Windows Terminal (which already works correctly)
# if [[ -z "$WT_SESSION" ]]; then
#     # Get the User PATH from Windows Registry
#     USER_PATH=$(powershell.exe -NoProfile -Command "[Environment]::GetEnvironmentVariable('Path', 'User')" 2>/dev/null | tr -d '\r')
    
#     if [[ -n "$USER_PATH" ]]; then
#         # Convert Windows path format to Unix format for Git Bash
#         # Changes: backslashes to forward slashes, drive letters (C:) to /c, semicolons to colons
#         USER_PATH_UNIX=$(echo "$USER_PATH" | sed 's|\\|/|g' | sed 's|\([A-Z]\):|/\L\1|g' | sed 's|;|:|g')
        
#         # Append User PATH to current PATH
#         export PATH="$PATH:$USER_PATH_UNIX"
#     fi
# fi

if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

export DEV="$HOME/dev"