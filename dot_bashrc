#####
# Non-interactive env (scripts, Claude Bash tool, etc.)
#####
export XDG_CONFIG_HOME="$HOME/.config"
export MSYS=winsymlinks:nativestrict
export PYTHONUTF8=1
export EDITOR="code --wait"
export VISUAL="code --wait"

export FZF_DEFAULT_COMMAND='fd --type f --strip-cwd-prefix'
export FZF_CTRL_T_OPTS="
    --walker-skip .git,node_modules,target,bin,obj
    --preview 'bat -n --color=always {}'
    --preview-window=right,80%
    --height 90%
    --bind 'ctrl-/:change-preview-window(down|)'"
export FZF_CTRL_R_OPTS="
    --bind 'ctrl-y:execute-silent(echo -n {2..} | clip)+abort'
    --color header:italic
    --header 'Press CTRL-Y to copy command into clipboard'
    --no-sort"
export FZF_ALT_C_OPTS="--walker-skip .git,node_modules,target,bin,obj"

# Exit early for non-interactive shells
[[ $- != *i* ]] && return

######
# aliases
######
alias l='lsd -l'
alias d=dotnet
alias cc=claude
alias ccch='claude --channels plugin:telegram@claude-plugins-official'
alias ccf='claude --fork-session'
alias oc='safe_tui opencode'
alias co='copilot'
alias oce='OPENCODE_EXPERIMENTAL_PLAN_MODE=1 safe_tui opencode'
alias ocm='bunx opencode-marketplace'
alias ccsp='claude --dangerously-skip-permissions'
alias c=code
alias vsci='code-insiders'
alias bashrc='code ~/.bashrc'
alias lg='lazygit'
alias ld='lazydocker'
alias lc='uvx lazyclaude'
alias loc='uvx lazyopencode'
alias g='git'
alias docker='podman'
alias cz='chezmoi'
alias vault='start obsidian://open?vault=notes'
alias cck='npx claude-code-kanban'
alias ccm='npx claude-code-marketplace'
alias ccc='npx claude-code-cost'
alias cch='npx claude-code-hub'
alias ??='opencode run --model google/gemini-3-flash-preview'
alias ???='opencode run --model google/gemini-3-flash-preview --continue'
alias wtc='wt -w 0 -d $(pwd) --title "$(basename $(pwd))"'
alias fzfd='fd --type d --strip-cwd-prefix | fzf'

######
# terminal
######
ac() {
    local dir="${1:-$(pwd)}"
    alacritty --working-directory "$dir" &
}

#update_terminal_title
utt() {
    echo -ne "\033]0;$(basename "$PWD")\007"
}

## well known issue with alacritty and mouse mode
safe_tui() {
    "$@"
    printf '\e[?1000l\e[?1002l\e[?1003l\e[?1006l'
}

fix_mouse() {
    printf '\e[?1000l\e[?1002l\e[?1003l\e[?1006l'
}

######
# tmux
######

# Usage: t [-n name] [folder] - attach/create tmux session named after folder (default: current dir)
t() {
  local name_override=""
  local OPTIND=1
  while getopts "n:" opt; do
    case $opt in
      n) name_override="$OPTARG" ;;
    esac
  done
  shift $((OPTIND - 1))

  local target_dir="${1:-$PWD}"
  local session_name="${name_override:-$(basename "$target_dir" | tr . _)}"

  if tmux has-session -t "=$session_name" 2>/dev/null; then
    if [ -n "$TMUX" ]; then
      tmux switch-client -t "=$session_name"
    else
      tmux attach-session -t "=$session_name"
    fi
  else
    if [ -n "$TMUX" ]; then
      tmux new-session -d -s "$session_name" -c "$target_dir"
      tmux switch-client -t "=$session_name"
    else
      tmux new-session -s "$session_name" -c "$target_dir"
    fi
  fi
}

# Tmux Dev Session Picker - fzf select from ~/dev, attach/create session
tdev() {
  local dir
  dir=$(fd . ~/dev -d 1 -t d | sed 's|C:/|/c/|i;s|/$||' | fzf --reverse)
  [[ -z "$dir" ]] && return 0
  t "$dir"
}

# Usage: tw [-n name] [folder] - create new tmux window named after folder (default: current dir)
tw() {
  local name_override=""
  local OPTIND=1
  while getopts "n:" opt; do
    case $opt in
      n) name_override="$OPTARG" ;;
    esac
  done
  shift $((OPTIND - 1))

  local target_dir="${1:-$PWD}"
  local window_name="${name_override:-$(basename "$target_dir" | tr . _)}"

  if [ -z "$TMUX" ]; then
    echo "Not in a tmux session"
    return 1
  fi

  tmux new-window -n "$window_name" -c "$target_dir"
}

######
# z
######
eval "$(zoxide init bash)"

######
# git
######
[ -f ~/.bashrc_git ] && source ~/.bashrc_git

######
# fzf
######
eval "$(fzf --bash)"

_fzf_history_dedup() {
    builtin history -a
    builtin history -c
    builtin history -r
    READLINE_LINE=$(
        HISTTIMEFORMAT='' builtin history |
        sed 's/^ *[0-9]* *//' |
        awk '!seen[$0]++' |
        fzf --tac --no-sort --scheme=history \
            --bind 'ctrl-y:execute-silent(echo -n {} | clip)+abort' \
            --color header:italic \
            --header 'Press CTRL-Y to copy command into clipboard' \
            --query "$READLINE_LINE"
    ) || true
    READLINE_POINT=${#READLINE_LINE}
}

bind -x '"\C-r": _fzf_history_dedup'

function fzfp() {
    local PROJECTS=$(dotnet sln list | sed '1,2d')
    echo "$PROJECTS" | fzf --preview "bat --style=numbers --color=always --line-range :500 {} -l xml"
}

rfzf() {
    fzf --disabled --ansi --query "$*" \
        --height 90% \
        --layout reverse \
        --preview-window 'down,80%,border-top,+{2}-5' \
        --delimiter : \
        --bind "start:reload:rg --column --line-number --no-heading --color=always --smart-case -- {q} || true" \
        --bind "change:reload:rg --column --line-number --no-heading --color=always --smart-case -- {q} || true" \
        --preview 'bat --style=numbers --color=always --highlight-line {2} {1}' \
        --bind 'ctrl-/:change-preview-window(right,60%|down,80%|hidden)' \
        --bind "ctrl-y:execute-silent(echo -n {1}:{2} | clip)" \
    | cut -d: -f1
}

cwt() {
  local selection=$(git worktree list | fzf \
    --header "ENTER=cd | CTRL-Y=copy path | CTRL-D=remove worktree" \
    --bind "ctrl-y:execute-silent(echo -n {1} | clip)+abort" \
    --bind "ctrl-d:execute(git worktree remove {1})+reload(git worktree list)" \
    --no-mouse)
  local dir=$(echo "$selection" | awk '{print $1}')
  [[ -n "$dir" ]] && cd "$dir"
}

######
# history
######
shopt -s histappend
HISTSIZE=5000
HISTFILESIZE=10000
HISTCONTROL=ignoreboth:erasedups
HISTIGNORE="ls:ll:l:cd:pwd:exit:clear"
PROMPT_COMMAND="history -a;$PROMPT_COMMAND"

######
# prompt
######
__git_branch() {
    local head gitdir=".git"
    [[ -e "$gitdir" ]] || return
    # Support submodules where .git is a file pointing to the real gitdir
    if [[ -f "$gitdir" ]]; then
        read -r _ gitdir < "$gitdir"
    fi
    read -r head < "$gitdir/HEAD" 2>/dev/null || return
    case "$head" in
        ref:*) printf ' \e[38;2;255;255;255mon\e[0m \e[38;2;255;148;223m\ue0a0 %s\e[0m' "${head#ref: refs/heads/}" ;;
        *) printf ' \e[38;2;255;255;255mon\e[0m \e[38;2;255;148;223m\ue0a0 %s\e[0m' "${head:0:7}" ;;
    esac
}
PROMPT_COMMAND='PS1="\[\e[36m\]\W\[\e[0m\]$(__git_branch)\n\[\e[92m\]❯\[\e[0m\] ";'"${PROMPT_COMMAND}"

# eval "$(oh-my-posh init bash --config ~/.nikiforovall.omp3.json)"

# Set terminal title to current directory on load
echo -ne "\033]0;$(basename "$PWD")\007"
